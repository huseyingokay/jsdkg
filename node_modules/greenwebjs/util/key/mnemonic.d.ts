import { bytes } from "../../xch/providers/provider_types";
export declare class MnemonicUtils {
    private static stdHash;
    private static hex2bin;
    private static bin2hex;
    static bytesToMnemonic(mnemonicBytes: bytes): string;
    static bytesFromMnemonic(mnemonicStr: string): bytes;
    static mnemonicToSeed(mnemonic: string, passphrase: string): string;
    static privateKeyFromMnemonic(mnemonic: string, passphrase?: string): any;
}
